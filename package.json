{
  "name": "@dream-sicle/workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "vp install",
    "standards:check": "node scripts/standards-check.mjs",
    "lint": "pnpm run standards:check",
    "ready": "pnpm run standards:check && vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run demo#dev"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.32.1"
}
