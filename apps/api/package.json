{
  "name": "@ds/api",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "clean": "bunx rimraf dist node_modules",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc",
    "dev": "bun run --watch --no-check --env-file ../../packages/db/.env.local --env-file .env.local src/server.ts"
  },
  "dependencies": {
    "@ds/contracts": "workspace:*",
    "@ds/db": "workspace:*",
    "@ds/utils": "workspace:*",
    "bcryptjs": "catalog:",
    "jsonwebtoken": "catalog:"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
